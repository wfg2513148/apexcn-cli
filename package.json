{
  "name": "apexcn-cli",
  "version": "0.4.0",
  "type": "module",
  "bin": {
    "apexcn": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:release": "node scripts/check-release-version.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "bundledDependencies": [
    "commander"
  ],
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
